package com.marcogn.kartlog.domain.model

/** Tema scelto in Impostazioni: quello di sistema o forzato chiaro/scuro (come ThePatientGamerHelper). */
enum class ThemeMode {
    SISTEMA,
    CHIARO,
    SCURO,
}
